name: Test WorkFlow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: this is a test
        run: echo "Hello this is my first action on github"
